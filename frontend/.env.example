# Base URL of the FastAPI backend. Leave empty when the frontend is served
# from the same origin as the API (recommended), or when using the Vite dev
# server (which proxies /api and /uploads to BACKEND_URL, default
# http://localhost:8000).
VITE_API_BASE_URL=
